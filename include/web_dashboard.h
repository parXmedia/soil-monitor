#pragma once

const char DASHBOARD_HTML[] PROGMEM = R"HTML(
<!doctype html><html lang="en"><head><meta charset="utf-8">
<meta name="viewport" content="width=device-width,initial-scale=1">
<title>Soil Monitor</title><style>
:root{--bg:#07110d;--panel:#102019;--panel2:#152a21;--text:#eefaf2;--muted:#91ae9c;--green:#48dc83;--yellow:#ffd166;--red:#ff6b6b;--cyan:#5ee7e7}
*{box-sizing:border-box}body{margin:0;background:radial-gradient(circle at top,#183729 0,var(--bg) 44%);color:var(--text);font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif;min-height:100vh}.wrap{width:min(1100px,calc(100% - 28px));margin:auto;padding:28px 0 48px}header{display:flex;align-items:center;justify-content:space-between;gap:16px;margin-bottom:22px}h1{font-size:clamp(25px,5vw,42px);margin:0;letter-spacing:-.04em}.sub{color:var(--muted);font-size:13px;margin-top:4px}.badge{padding:8px 13px;border-radius:999px;background:#25352d;color:var(--muted);font-weight:800;font-size:12px;letter-spacing:.07em}.badge.fresh{background:#123d25;color:var(--green)}.badge.off{background:#431d20;color:var(--red)}.panel{background:linear-gradient(145deg,rgba(23,48,37,.96),rgba(12,27,20,.98));border:1px solid #284638;border-radius:22px;box-shadow:0 20px 70px #0007}.hero{display:grid;grid-template-columns:1fr 1.15fr;gap:22px;padding:26px;margin-bottom:18px}.moist-label,.eyebrow{color:var(--muted);font-size:12px;font-weight:800;letter-spacing:.12em;text-transform:uppercase}.moist{font-size:clamp(70px,13vw,128px);font-weight:900;letter-spacing:-.08em;line-height:.95;margin:11px 0 14px}.condition{font-size:18px;font-weight:800}.bar{height:13px;background:#08130e;border-radius:20px;overflow:hidden;margin-top:18px}.bar>i{display:block;width:0;height:100%;border-radius:20px;background:var(--green);transition:.6s}.analysis{display:flex;flex-direction:column;justify-content:center;background:#0b1913;border:1px solid #233e31;border-radius:17px;padding:22px}.analysis h2{font-size:23px;margin:7px 0 9px}.analysis p{color:#b8d1c1;line-height:1.55;margin:0}.grid{display:grid;grid-template-columns:repeat(4,1fr);gap:12px;margin-bottom:18px}.card{padding:18px;min-height:118px}.value{font-weight:850;font-size:27px;margin-top:12px;letter-spacing:-.03em}.detail{color:var(--muted);font-size:12px;margin-top:5px}.mode-control{display:flex;align-items:center;gap:10px;margin-top:11px;cursor:pointer;font-size:13px;font-weight:850;color:var(--text)}.mode-control input{position:absolute;opacity:0;pointer-events:none}.toggle-track{position:relative;display:inline-block;width:48px;height:26px;flex:0 0 48px;border-radius:999px;background:#394b42;border:1px solid #617269;transition:.2s}.toggle-track:after{content:'';position:absolute;width:20px;height:20px;left:2px;top:2px;border-radius:50%;background:#d5ded8;box-shadow:0 2px 6px #0008;transition:.2s}.mode-control input:checked+.toggle-track{background:#16743d;border-color:var(--green)}.mode-control input:checked+.toggle-track:after{transform:translateX(22px);background:#fff}.mode-control input:focus-visible+.toggle-track{outline:3px solid var(--cyan);outline-offset:3px}.mode-control:has(input:disabled){cursor:wait;opacity:.6}.cal-row{display:flex;align-items:center;gap:6px;margin-top:10px;flex-wrap:wrap}.cal-row label{font-size:12px;color:var(--muted);font-weight:800}.cal-row input{width:72px;padding:7px;border-radius:8px;border:1px solid #42644f;background:#09150f;color:var(--text);font-size:14px}.cal-row button{border:0;border-radius:8px;padding:8px 12px;background:#48dc83;color:#06200f;font-weight:900;font-size:13px;cursor:pointer}.cal-row button:disabled{opacity:.5;cursor:wait}.chart{padding:22px;margin-bottom:18px}.chart-head{display:flex;justify-content:space-between;align-items:end;margin-bottom:12px}.chart h2{margin:5px 0 0;font-size:20px}.legend{color:var(--muted);font-size:12px}canvas{display:block;width:100%;height:260px;background:#091611;border-radius:13px}.foot{display:flex;justify-content:space-between;gap:12px;color:var(--muted);font-size:12px;padding:0 4px}.foot a{color:var(--cyan)}@media(max-width:760px){.hero{grid-template-columns:1fr}.grid{grid-template-columns:repeat(2,1fr)}}@media(max-width:420px){.grid{grid-template-columns:1fr}.hero{padding:18px}.wrap{width:min(100% - 18px,1100px)}}
.cal-live{margin-top:9px;padding:8px 10px;border-radius:9px;background:#09150f;color:var(--cyan);font-size:13px;font-weight:800}.cal-step{margin-top:10px;padding:10px;border:1px solid #2b493a;border-radius:10px;background:#0b1913}.cal-step strong{display:block;font-size:13px}.cal-step p{margin:4px 0 8px;color:var(--muted);font-size:11px;line-height:1.4}.cal-capture{display:flex;align-items:center;gap:8px}.cal-reading{min-width:78px;padding:7px;border-radius:8px;background:#07110d;color:var(--text);font-weight:900;text-align:center}.cal-capture button,.cal-guide-save{border:0;border-radius:8px;padding:8px 12px;background:#28523b;color:var(--text);font-weight:900;font-size:12px;cursor:pointer}.cal-guide-save{width:100%;margin-top:10px;background:#48dc83;color:#06200f}.cal-capture button:disabled,.cal-guide-save:disabled{opacity:.45;cursor:not-allowed}.cal-warning{color:var(--yellow);font-size:11px;line-height:1.4;margin-top:8px}details{margin-top:9px;color:var(--muted);font-size:11px}summary{cursor:pointer}
.modes{display:grid;grid-template-columns:repeat(3,1fr);gap:5px;margin-top:12px}.modes button,.action,.upload-label{border:1px solid #42644f;border-radius:9px;padding:9px 7px;background:#0a1711;color:var(--text);font-weight:850;font-size:12px;cursor:pointer;text-align:center}.modes button.active{background:#16743d;border-color:var(--green)}.modes button:disabled,.action:disabled{opacity:.5;cursor:wait}.actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:11px}.action.danger{border-color:#7c3b3b;color:#ffaaaa}.update-form{display:grid;gap:8px;margin-top:10px}.update-form input{width:100%;padding:9px;border-radius:8px;border:1px solid #42644f;background:#09150f;color:var(--text)}.update-form small{color:var(--muted);line-height:1.4}.power-estimate{color:var(--yellow)}
</style></head><body><div class="wrap">
<header><div><h1>Soil Monitor</h1><div class="sub">Long-range ESP-NOW garden telemetry</div></div><div id="status" class="badge">CONNECTING</div></header>
<section class="panel hero"><div><div class="moist-label">Current moisture</div><div id="moist" class="moist">--%</div><div id="condition" class="condition">Waiting for sensor</div><div class="bar"><i id="moistBar"></i></div></div><div class="analysis"><div class="eyebrow">Plant insight</div><h2 id="analysisTitle">Collecting data</h2><p id="analysisText">The dashboard will generate an interpretation as soon as the first sensor packet arrives.</p></div></section>
<section class="grid">
 <div class="panel card"><div class="eyebrow">1-hour average</div><div id="avg" class="value">--%</div><div id="range" class="detail">Range --</div></div>
 <div class="panel card"><div class="eyebrow">Trend</div><div id="trend" class="value">--</div><div id="delta" class="detail">Change --</div></div>
 <div class="panel card"><div class="eyebrow">Wireless strength</div><div id="signal" class="value">--%</div><div id="rssi" class="detail">RSSI -- dBm</div></div>
 <div class="panel card"><div class="eyebrow">Packet reliability</div><div id="reliability" class="value">--%</div><div id="packets" class="detail">0 packets</div></div>
 <div class="panel card"><div class="eyebrow">Sensor output</div><div id="voltage" class="value">-- V</div><div id="raw" class="detail">Raw ADC --</div></div>
 <div class="panel card"><div class="eyebrow">Last sensor update</div><div id="age" class="value">--</div><div id="sequence" class="detail">Never received</div></div>
 <div class="panel card"><div class="eyebrow">Sample history</div><div id="samples" class="value">0</div><div class="detail">Stored trend samples</div></div>
 <div class="panel card"><div class="eyebrow">Sampling mode</div><div id="gateway" class="value">Loading</div><label class="mode-control" for="modeToggle"><input id="modeToggle" type="checkbox" role="switch" aria-label="Toggle instant sensor readings"><span class="toggle-track" aria-hidden="true"></span><span id="modeToggleLabel">Instant · every 2 seconds</span></label><div class="detail">Off: every 5 minutes · On: instant updates</div><div id="modeDetail" class="detail" aria-live="polite">Waiting for gateway</div><div id="ip" class="detail">Loading address</div></div>
 <div class="panel card"><div class="eyebrow">Two-point calibration</div><div id="calState" class="value">--</div><div id="calLive" class="cal-live">Waiting for a fresh sensor reading</div><div class="cal-step"><strong>1. Open-air reading</strong><p>Remove the probe from soil, keep it dry and still, then collect three new readings.</p><div class="cal-capture"><span id="calGuidedDry" class="cal-reading">Not captured</span><button id="calCaptureDry" type="button" disabled>Capture air</button></div></div><div class="cal-step"><strong>2. Water reading</strong><p>Immerse only the sensing section, keep the connector dry, wait for the raw value to settle, then collect three readings.</p><div class="cal-capture"><span id="calGuidedWet" class="cal-reading">Not captured</span><button id="calCaptureWet" type="button" disabled>Capture water</button></div></div><button id="calGuidedSave" class="cal-guide-save" type="button" disabled>Save two-point calibration</button><div id="calGuideDetail" class="detail" aria-live="polite">Nothing is being collected. Turn on Instant readings, position the probe, then press a Capture button.</div><div class="cal-warning">Calibration starts only when you press Capture. It is saved only after both stable points are collected. Never immerse the connector or electronics.</div><div id="calDetail" class="detail" aria-live="polite">Loading saved calibration</div></div>
 <div class="panel card"><div class="eyebrow">Cloud upload</div><div id="cloudState" class="value">--</div><div id="cloudPending" class="detail">Buffered --</div><div id="cloudDropped" class="detail">Dropped --</div></div>
 <div class="panel card"><div class="eyebrow">Live power draw</div><div id="power" class="value">-- mW</div><div id="current" class="detail">Available in Live mode</div><div id="powerKind" class="detail">Current-sense status unknown</div></div>
 <div class="panel card"><div class="eyebrow">Sensor health</div><div id="battery" class="value">-- V</div><div id="firmware" class="detail">Firmware build --</div><div id="protocol" class="detail">Protocol --</div></div>
 <div class="panel card"><div class="eyebrow">Sensor wireless update</div><div id="updateState" class="value">Ready</div><form id="updateForm" class="update-form"><input id="updateBuild" name="build" inputmode="numeric" placeholder="Build ID, e.g. 0x00030001" required><input id="updateFile" name="firmware" type="file" accept=".bin,application/octet-stream" required><button class="action" type="submit">Stage update for next check-in</button><small id="updateDetail">Upload the sensor_transmitter firmware .bin. The encrypted handshake tells the sensor when to download it.</small></form><button id="cancelUpdate" class="action danger" type="button" hidden>Cancel staged update</button></div>
 <div class="panel card"><div class="eyebrow">Gateway maintenance</div><div class="value">Controls</div><div class="actions"><button id="refreshNow" class="action" type="button">Refresh now</button><button id="clearHistory" class="action" type="button">Clear local history</button><button id="resetCalibration" class="action danger" type="button">Reset calibration</button><button id="restartGateway" class="action danger" type="button">Restart gateway</button></div><div id="controlDetail" class="detail" aria-live="polite">Gateway firmware loading</div></div>
</section>
<section class="panel chart"><div class="chart-head"><div><div class="eyebrow">Moisture history</div><h2>Last 60 minutes</h2></div><div class="legend">0% dry · 100% wet</div></div><canvas id="plot"></canvas></section>
<div class="foot"><span>Refreshes every 2 seconds · <b>soil-monitor.local</b></span><span>Hold BOOT during reset to change Wi-Fi</span></div>
</div><script>
const $=id=>document.getElementById(id);let historyData=[],modeSaving=false,calSaving=false;
let latestCalibrationReading=null,calCaptureTarget=null,calCaptureSamples=[],calCaptureLastSequence=null,guidedDry=null,guidedWet=null;
function setCalibrationControls(){const usable=latestCalibrationReading&&latestCalibrationReading.fresh&&latestCalibrationReading.valid&&!calSaving,active=calCaptureTarget!==null;$('calCaptureDry').disabled=active?calCaptureTarget!=='dry':!usable;$('calCaptureWet').disabled=active?calCaptureTarget!=='wet':!usable;$('calCaptureDry').textContent=calCaptureTarget==='dry'?'Cancel':'Capture air';$('calCaptureWet').textContent=calCaptureTarget==='wet'?'Cancel':'Capture water';$('calGuidedSave').disabled=calSaving||active||guidedDry===null||guidedWet===null}
function finishCalibrationCapture(target,value,spread){if(target==='dry'){guidedDry=value;$('calGuidedDry').textContent=value+' raw'}else{guidedWet=value;$('calGuidedWet').textContent=value+' raw'}calCaptureTarget=null;calCaptureSamples=[];calCaptureLastSequence=null;$('calGuideDetail').textContent='Captured stable '+(target==='dry'?'open-air':'water')+' point '+value+' (spread '+spread+'). '+(guidedDry===null||guidedWet===null?'Capture the other point next.':'Both points are ready; review and save.');setCalibrationControls()}
function collectCalibrationSample(d){if(!calCaptureTarget||!d.fresh||!d.valid||d.sequence===calCaptureLastSequence)return;calCaptureLastSequence=d.sequence;calCaptureSamples.push(d.raw);if(calCaptureSamples.length>3)calCaptureSamples.shift();if(calCaptureSamples.length<3){$('calGuideDetail').textContent='Collecting '+calCaptureTarget+' readings: '+calCaptureSamples.length+'/3. Keep the probe still.';return}const ordered=[...calCaptureSamples].sort((a,b)=>a-b),spread=ordered[2]-ordered[0];if(spread>80){$('calGuideDetail').textContent='Readings are still moving (spread '+spread+'). Keep the probe still; collecting another sample.';return}finishCalibrationCapture(calCaptureTarget,ordered[1],spread)}
async function calibrationLive(){try{const d=await fetch('/api/data',{cache:'no-store'}).then(r=>r.json());latestCalibrationReading=d;const usable=d.received>0&&d.age_ms<4294967295&&d.fresh&&d.valid;$('calLive').textContent=usable?'Live raw '+d.raw+' · '+ageText(d.age_ms):'No fresh valid reading — latest raw '+(d.received>0?d.raw:'--');collectCalibrationSample(d);setCalibrationControls()}catch(e){latestCalibrationReading=null;$('calLive').textContent='Gateway reading unavailable';setCalibrationControls()}}
function beginCalibrationCapture(target){if(calCaptureTarget===target){calCaptureTarget=null;calCaptureSamples=[];calCaptureLastSequence=null;$('calGuideDetail').textContent='Capture cancelled.';setCalibrationControls();return}const d=latestCalibrationReading;if(!d||!d.fresh||!d.valid){$('calGuideDetail').textContent='Wait for a fresh valid sensor reading before capturing.';return}calCaptureTarget=target;calCaptureSamples=[];calCaptureLastSequence=d.sequence;$('calGuideDetail').textContent='Started '+(target==='dry'?'open-air':'water')+' capture. Waiting for 3 new sensor readings…';setCalibrationControls()}
async function saveGuidedCalibration(){if(calSaving||guidedDry===null||guidedWet===null)return;if(guidedDry<=guidedWet+200){$('calGuideDetail').textContent='Calibration rejected: open-air raw must exceed water raw by at least 200 counts.';return}calSaving=true;setCalibrationControls();$('calGuideDetail').textContent='Saving calibration…';try{const r=await fetch('/api/calibration',{method:'POST',cache:'no-store',headers:{'Content-Type':'application/json','X-Soil-Control':'local-dashboard'},body:JSON.stringify({dry:guidedDry,wet:guidedWet})});if(!r.ok)throw new Error();$('calGuideDetail').textContent='Calibration saved. Percentages, LCD, new history, and future cloud uploads now use these points.';guidedDry=null;guidedWet=null;$('calGuidedDry').textContent='Not captured';$('calGuidedWet').textContent='Not captured';await live()}catch(e){$('calGuideDetail').textContent='Save failed — the gateway rejected these readings.'}finally{calSaving=false;setCalibrationControls()}}
function colorFor(v){return v<25?'var(--red)':v<45?'var(--yellow)':'var(--green)'}
function insight(v,trend){if(v<25)return['Water recommended','The soil is dry. Water the plant and confirm that the reading begins to rise.'];if(v<45)return['Moisture is getting low',trend==='Falling'?'The soil is drying. Plan to water soon if the downward trend continues.':'The soil is slightly dry but currently usable. Watch the trend.'];if(v<80)return['Moisture is in the healthy range','No watering is indicated from the current reading. Continue monitoring the one-hour trend.'];return['Soil is very wet','Pause watering and verify drainage. Persistently high moisture may stress roots.']}
function ageText(ms){const s=Math.max(0,Math.round(ms/1000));if(s<10)return'just now';if(s<60)return s+'s ago';const m=Math.round(s/60);if(m<60)return m+'m ago';const h=Math.round(m/60);return h<24?h+'h ago':Math.round(h/24)+'d ago'}
function updateTime(ms){const t=new Date(Date.now()-Math.max(0,ms));return ms<86400000?t.toLocaleTimeString([],{hour:'numeric',minute:'2-digit',second:'2-digit'}):t.toLocaleString()}
async function live(){try{const d=await fetch('/api/data',{cache:'no-store'}).then(r=>r.json()),hasReading=d.received>0&&d.age_ms<4294967295,fresh=hasReading&&d.fresh===true,labels={live:'Instant read',low_power:'5 minutes'},instant=d.sampling_mode==='live';$('status').textContent=fresh?'FRESH':'STALE';$('status').className='badge '+(fresh?'fresh':'off');$('moist').textContent=d.valid?d.moisture+'%':'--%';$('condition').textContent=d.valid?d.condition:'Check sensor wiring';$('moist').style.color=d.valid?colorFor(d.moisture):'var(--red)';$('moistBar').style.width=(d.valid?d.moisture:0)+'%';$('moistBar').style.background=colorFor(d.moisture);$('avg').textContent=d.samples?d.average.toFixed(1)+'%':'--%';$('range').textContent=d.samples?'Range '+d.minimum+'–'+d.maximum+'%':'Range --';$('trend').textContent=d.trend;$('delta').textContent=(d.delta>=0?'+':'')+d.delta.toFixed(1)+' points';$('signal').textContent=d.signal+'%';$('rssi').textContent='RSSI '+d.rssi+' dBm';$('reliability').textContent=d.reliability.toFixed(1)+'%';$('packets').textContent=d.received+' received · '+d.missed+' missed';$('voltage').textContent=(d.millivolts/1000).toFixed(2)+' V';$('raw').textContent='Raw ADC '+d.raw;$('age').textContent=hasReading?updateTime(d.age_ms):'Never';$('sequence').textContent=hasReading?ageText(d.age_ms)+' · Sequence '+d.sequence:'No packet since gateway restart';$('samples').textContent=d.samples;$('gateway').textContent=labels[d.sampling_mode]||d.sampling_mode;$('modeToggle').checked=instant;$('modeToggle').disabled=modeSaving;$('modeToggleLabel').textContent=instant?'Instant · every 2 seconds':'Every 5 minutes';$('modeDetail').textContent=!hasReading?'The choice applies when the sensor checks in':d.next_sample_seconds===d.requested_sample_seconds?'Sensor confirmed this mode':'Applying mode to sensor…';$('ip').textContent=d.ip;$('calState').textContent=d.calibrated?'Custom':'Defaults';$('calDetail').textContent=calSaving?'Saving…':(d.calibrated?'Saved: dry '+d.calibration_dry+' · wet '+d.calibration_wet:'Using firmware defaults until a guided calibration is saved');$('cloudState').textContent=d.cloud_online?'Online':'Buffering';$('cloudPending').textContent='Buffered '+d.cloud_pending+' ('+d.cloud_buffer+')';$('cloudDropped').textContent='Dropped '+d.cloud_dropped;$('battery').textContent=d.battery_millivolts?(d.battery_millivolts/1000).toFixed(2)+' V':'Unavailable';$('firmware').textContent='Sensor build '+(d.sensor_firmware_build||'--')+' · Gateway '+d.gateway_firmware;$('protocol').textContent='Radio protocol v'+d.sensor_protocol;if(d.power_available){$('power').textContent=d.power_milliwatts+' mW';$('current').textContent=d.current_milliamps+' mA';$('powerKind').textContent=d.power_measured?'Measured by current-sense input':'Estimated — fit a current sensor for measurement';$('powerKind').className='detail '+(d.power_measured?'':'power-estimate')}else{$('power').textContent='-- mW';$('current').textContent=d.sampling_mode==='live'?'Waiting for next live packet':'Available in Live mode';$('powerKind').textContent='No power telemetry in sleep modes'}$('updateState').textContent=d.sensor_update_uploading?'Uploading':(d.sensor_update_ready?'Staged':'Ready');$('updateDetail').textContent=d.sensor_update_error|| (d.sensor_update_ready?'Build '+d.sensor_update_build+' · '+Math.round(d.sensor_update_size/1024)+' KiB · waiting for sensor handshake':'Upload sensor_transmitter firmware for wireless installation');$('cancelUpdate').hidden=!d.sensor_update_ready;$('controlDetail').textContent='Gateway '+d.gateway_firmware+' · '+d.ip;const i=insight(d.moisture,d.trend);$('analysisTitle').textContent=i[0];$('analysisText').textContent=i[1]}catch(e){$('status').textContent='GATEWAY ERROR';$('status').className='badge off'}}
async function setSamplingMode(mode){if(modeSaving)return;modeSaving=true;$('modeToggle').disabled=true;$('modeDetail').textContent='Applying '+(mode==='live'?'instant':'5-minute')+' mode…';try{const r=await control('/api/mode',{mode});if(!r.ok)throw new Error()}catch(e){$('modeDetail').textContent='Mode change failed — previous setting kept'}finally{modeSaving=false;await live()}}
async function control(path,body={},method='POST'){return fetch(path,{method,cache:'no-store',headers:{'Content-Type':'application/json','X-Soil-Control':'local-dashboard'},body:method==='DELETE'?undefined:JSON.stringify(body)})}
async function runControl(path,message){$('controlDetail').textContent=message;try{const r=await control(path);if(!r.ok)throw new Error();$('controlDetail').textContent='Done';await Promise.all([live(),history()])}catch{$('controlDetail').textContent='Control failed'}}
async function uploadSensorUpdate(event){event.preventDefault();const file=$('updateFile').files[0],build=$('updateBuild').value.trim();if(!file||!build)return;$('updateDetail').textContent='Uploading '+file.name+'…';const form=new FormData();form.append('build',build);form.append('firmware',file,file.name);try{const r=await fetch('/api/sensor-update',{method:'POST',headers:{'X-Soil-Control':'local-dashboard'},body:form});if(!r.ok)throw new Error();$('updateDetail').textContent='Update staged. It will install at the next sensor handshake.';$('updateForm').reset();await live()}catch{$('updateDetail').textContent='Upload failed. Use a sensor_transmitter .bin and a nonzero build ID.'}}
async function history(){try{historyData=await fetch('/api/history',{cache:'no-store'}).then(r=>r.json());draw()}catch(e){}}
function draw(){const c=$('plot'),r=c.getBoundingClientRect(),q=devicePixelRatio||1;c.width=r.width*q;c.height=r.height*q;const x=c.getContext('2d');x.scale(q,q);const w=r.width,h=r.height,p=38;x.clearRect(0,0,w,h);x.strokeStyle='#294438';x.fillStyle='#729080';x.font='11px system-ui';x.lineWidth=1;[0,25,50,75,100].forEach(v=>{const y=h-p-(v/100)*(h-p*1.45);x.beginPath();x.moveTo(p,y);x.lineTo(w-12,y);x.stroke();x.fillText(v+'%',4,y+4)});const a=historyData.moisture||[];if(a.length<2)return;x.strokeStyle='#48dc83';x.lineWidth=3;x.lineJoin='round';x.beginPath();a.forEach((v,i)=>{const xx=p+i*(w-p-14)/(a.length-1),yy=h-p-(v/100)*(h-p*1.45);i?x.lineTo(xx,yy):x.moveTo(xx,yy)});x.stroke();x.fillStyle='#729080';x.fillText('60m ago',p,h-10);x.fillText('now',w-34,h-10)}
addEventListener('resize',draw);$('modeToggle').addEventListener('change',event=>setSamplingMode(event.target.checked?'live':'low_power'));$('calCaptureDry').addEventListener('click',()=>beginCalibrationCapture('dry'));$('calCaptureWet').addEventListener('click',()=>beginCalibrationCapture('wet'));$('calGuidedSave').addEventListener('click',saveGuidedCalibration);$('updateForm').addEventListener('submit',uploadSensorUpdate);$('cancelUpdate').addEventListener('click',async()=>{await control('/api/sensor-update',{},'DELETE');await live()});$('refreshNow').addEventListener('click',()=>Promise.all([live(),history()]));$('clearHistory').addEventListener('click',()=>runControl('/api/history/clear','Clearing local history…'));$('resetCalibration').addEventListener('click',()=>runControl('/api/calibration/reset','Resetting calibration…'));$('restartGateway').addEventListener('click',()=>runControl('/api/restart','Restarting gateway…'));live();calibrationLive();history();setInterval(live,2000);setInterval(calibrationLive,2000);setInterval(history,10000);
</script></body></html>
)HTML";

const char WIFI_SETUP_HTML[] PROGMEM = R"HTML(
<!doctype html><html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Soil Monitor Wi-Fi Setup</title><style>
body{margin:0;background:#07110d;color:#eefaf2;font-family:system-ui;display:grid;place-items:center;min-height:100vh}.box{width:min(430px,calc(100% - 32px));background:#13271e;border:1px solid #315441;border-radius:22px;padding:28px;box-sizing:border-box}h1{margin:0 0 8px}p{color:#a9c3b3;line-height:1.45}label{display:block;margin:18px 0 6px;font-weight:700}input{width:100%;box-sizing:border-box;padding:13px;border-radius:10px;border:1px solid #42644f;background:#09150f;color:white;font-size:16px}button{width:100%;margin-top:22px;border:0;border-radius:11px;padding:14px;background:#48dc83;color:#06200f;font-weight:900;font-size:16px}.note{font-size:12px;color:#8cab98;margin-top:16px}a{color:#5ee7e7}
</style></head><body><form class="box" method="post" action="/save-wifi"><h1>Soil Monitor</h1><p>Connect the display gateway to your home Wi-Fi. It will restart and become available at <b>http://soil-monitor.local</b>.</p><label>Home Wi-Fi name (SSID)</label><input name="ssid" maxlength="32" required autocomplete="off"><label>Wi-Fi password</label><input name="password" type="password" maxlength="63" autocomplete="new-password"><h2>Secure cloud upload</h2><p>Leave these three fields empty until the cloud API is deployed. Existing cloud settings are preserved when they are blank.</p><label>Cloud ingest URL</label><input name="cloud_api_url" type="url" maxlength="220" placeholder="https://…/soil-api/v1/ingest" autocomplete="off"><label>Cloud device ID</label><input name="cloud_device_id" maxlength="64" placeholder="garden-sensor-1" autocomplete="off"><label>Cloud ingest secret</label><input name="cloud_ingest_secret" type="password" minlength="32" maxlength="256" autocomplete="new-password"><h2>Wireless firmware updates</h2><p>Optional. Set a password of at least 12 characters to allow future firmware uploads over Wi-Fi instead of USB. Leave blank to keep updates USB-only.</p><label>OTA update password</label><input name="ota_password" type="password" minlength="12" maxlength="64" autocomplete="new-password"><button type="submit">Save and connect</button><div class="note">Credentials stay in the gateway's local flash and are never sent to GitHub Pages. Use a 2.4 GHz network; ESP32-S3 does not connect to 5 GHz-only Wi-Fi.</div></form></body></html>
)HTML";
